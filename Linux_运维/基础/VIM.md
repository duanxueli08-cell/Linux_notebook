
---

# ✅ 一、Vim 配置文件在哪里？

Vim 的配置文件一般有两个：

### **1. 用户级配置（最常用）**

```
~/.vimrc
```

只影响当前用户。

### **2. 系统级配置**

```
/etc/vim/vimrc
```

影响所有用户（一般不建议修改）。

> **日常 99% 情况修改 ~/.vimrc 即可。**

---

# ✅ 二、如何编辑 Vim 配置文件？

```bash
vim ~/.vimrc
```

第一次会是空文件，直接写入配置即可。

修改完成后保存退出；加载配置文件使其立刻生效；

```
source ~/.vimrc
```

---

# ✅ 三、生产环境最常用 Vim 配置示例（推荐）

你可以全部复制到 `~/.vimrc`：

```vim
" 显示行号
set number

" 显示光标当前位置
set ruler

" 启用鼠标
set mouse=a

" 开启语法高亮
syntax on

" 设置 Tab 为4个空格
set tabstop=4
set shiftwidth=4
set expandtab

" 自动缩进
set autoindent
set smartindent

" 搜索高亮
set hlsearch
set incsearch
set ignorecase       " 忽略大小写
set smartcase        " 仅在有大写字母时区分大小写

" 显示匹配的括号
set showmatch

" 取消自动换行
set nowrap

" 让Backspace真正有用
set backspace=indent,eol,start

" 关闭按键提示声音
set noerrorbells
set novisualbell

" 文件自动检测外部修改
set autoread

" 文件编码
set encoding=utf-8
set fileencoding=utf-8

" 设置搜索结果数量
set showcmd

" 显示状态栏
set laststatus=2
```

---

# ✅ 四、进阶配置（选用）

---

## 🔹1. 强制使用 UTF-8

```vim
set encoding=utf-8
set fileencodings=utf-8,gbk,ucs-bom,cp936
```

---

## 🔹2. 自动保存（不是强制）

```vim
autocmd FocusLost * :wa
```

---

## 🔹3. 显示不可见字符（例如 Tab）

```vim
set list
set listchars=tab:»·,trail:·
```

---

## 🔹4. 粘贴模式防止格式乱掉

（特别是在复制代码时）

```vim
set pastetoggle=<F2>
```

按 F2 进入粘贴模式，再次按 F2 退出。

---

# ✅ 五、如何还原默认配置？

```bash
mv ~/.vimrc ~/.vimrc.bak
```

Vim 就会恢复默认效果。

---

# ✅ 六、检查 Vim 当前生效配置

```bash
:scriptnames
```

查看加载了哪些配置文件。

---


## 替换（超常用）
| 示例           | 说明         |
| ------------ | ---------- |
| `:%s/a/b/g`  | 全文替换 a → b |
| `:%s/a/b/gc` | 全文替换并确认    |
| `:s/a/b/g`   | 替换当前行      |

**天阶功法**
```
Ctrl + v → 选中多行 → I → 输入 # → Esc
```



**在 vim 的普通模式下，`r` 就是“单字符快速替换”的快捷键**。

### ✔ `r` 的作用

- 用来 **替换光标所在位置的单个字符**
    
- 无需进入插入模式
    
- 输入方式：
    
    `r + 新字符`
    
    例如：光标在字母 `a` 上，按下 `r b` ➜ 会把 `a` 替换为 `b`。

### 推荐记住的 “最常用组合”

- **`diw`**（delete inner word）  
    无论光标在单词中任何位置，直接删除整个单词。
    
- **`daw`**（delete a word）
    比 `diw` 多删掉一个空格，适合清理语句。